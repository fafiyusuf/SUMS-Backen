// import express from 'express';
// import incidentController from '../controllers/incidentController';
// import verifyToken from '../middleware/authMiddleware';

// const router = express.Router();

// router.post('/', verifyToken, (req, res, next) =>
//   incidentController.reportIncident(req, res, next)
// );

// router.get('/:id', verifyToken, (req, res, next) =>
//   incidentController.getIncident(req, res, next)
// );

// router.get('/', verifyToken, (req, res, next) =>
//   incidentController.getAllIncidents(req, res, next)
// );

// router.put('/:id/status', verifyToken, (req, res, next) =>
//   incidentController.updateIncidentStatus(req, res, next)
// );

// export default router;
